# syntax=dhi.io/build:2-helm3

name: CloudNativePG Helm chart 0.x
image: dhi.io/cloudnative-pg-chart
variant: helm
tags:
  - 0.28.2
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 1cc064ac89730116588783ef2f85cf0af9406e26
  CLOUDNATIVE_PG_REFERENCE: dhi/cloudnative-pg:1.29.1-debian13@sha256:cd2c16b0d7994cd5299d667d0bf2f2963a8101dc2acca092a2a713275d20b8f3
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:f4a916b146b07955a0f7f705854bfda3af7853e4cedb839335d4d269004cbf00
  SEMVER_MAJOR_MINOR_VERSION: "0.28"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.28.2
  VERSION: 0.28.2
contents:
  builds:
    - name: cloudnative-pg
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:f4a916b146b07955a0f7f705854bfda3af7853e4cedb839335d4d269004cbf00
      contents:
        packages:
          - bash
          - build-essential
          - coreutils
          - grep
          - libc-bin
          - sed
        files:
          - url: git+https://github.com/cloudnative-pg/charts.git#cloudnative-pg-v0.28.2
            checksum: 1cc064ac89730116588783ef2f85cf0af9406e26
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/cloudnative-pg
          with:
            chart:
              appRef: cloudnative-pg
              description: Docker Hardened Images Helm chart for CloudNativePG
              images:
                - name: cloudnative-pg
                  ref: dhi/cloudnative-pg:1.29.1-debian13@sha256:cd2c16b0d7994cd5299d667d0bf2f2963a8101dc2acca092a2a713275d20b8f3
              name: cloudnative-pg-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/cloudnative-pg/charts
              version: 0.28.2
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/charts/cloudnative-pg
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: helm login
          uses: helm/login@v1
          with:
            registry: dhi.io
        - name: pull dependencies
          uses: helm/cmd@v1
          privileged: true
          with:
            chart: ${source.dir}/helm-charts/charts/cloudnative-pg
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: cloudnative-pg
            cmd: lint .
        - name: package
          uses: helm/package@v1
          work-dir: ${source.dir}/helm-charts/charts
          with:
            chart: cloudnative-pg
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/cloudnative-pg/config
