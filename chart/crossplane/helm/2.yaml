# syntax=dhi.io/build:2-helm3

name: Crossplane Helm chart 2.x
image: dhi.io/crossplane-chart
variant: helm
tags:
  - 2.2.1
platforms:
  - linux/amd64
vars:
  CHART_COMMIT_SHA: 713541df7fc5cf0946b6573837831086465a2dbe
  CROSSPLANE_REFERENCE: dhi/crossplane:2.2.1-debian13@sha256:93e40b03a49989d008609c0f053b82680ad2f1b89559997cbfc265d948a5052b
  DHI_NAMESPACE: dhi
  DHI_PREFIX: ""
  DHI_REGISTRY: dhi.io
  DHI_SKIP_CONTEXT_COPY: "true"
  HELM_REFERENCE: dhi/helm:3.21.0-debian13-dev@sha256:462393863790e8d4b174f63f0da50798da095a059d4fab7692ae4fb7a58e99d3
  SEMVER_MAJOR_MINOR_VERSION: "2.2"
  SEMVER_MAJOR_VERSION: "2"
  SEMVER_VERSION: 2.2.1
  VERSION: 2.2.1
contents:
  builds:
    - name: crossplane
      uses: dhi.io/helm:3.21.0-debian13-dev@sha256:462393863790e8d4b174f63f0da50798da095a059d4fab7692ae4fb7a58e99d3
      contents:
        files:
          - url: git+https://github.com/crossplane/crossplane.git#v2.2.1
            checksum: 713541df7fc5cf0946b6573837831086465a2dbe
            path: ${source.dir}/helm-charts
            uid: 0
            gid: 0
      pipeline:
        - name: prepare chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster/charts/crossplane
          with:
            chart:
              appRef: crossplane
              description: Docker Hardened Images Helm chart for Crossplane
              images:
                - name: crossplane
                  ref: dhi/crossplane:2.2.1-debian13@sha256:93e40b03a49989d008609c0f053b82680ad2f1b89559997cbfc265d948a5052b
              name: crossplane-chart
              notes: ${source.dir}/config/NOTES.txt
              sourceUrl: https://github.com/crossplane/crossplane
            cmd: prepare
            registry: dhi.io
        - name: relocate chart
          uses: helm/rudder@v1
          work-dir: ${source.dir}/helm-charts/cluster/charts/crossplane
          with:
            cmd: patch
            patches:
              from: ${source.dir}/config
            registry: dhi.io
        - name: update dependencies
          uses: helm/cmd@v1
          with:
            chart: ${source.dir}/helm-charts/cluster/charts/crossplane
            cmd: dependency update
        - name: lint
          uses: helm/cmd@v1
          work-dir: ${source.dir}/helm-charts/cluster/charts
          with:
            chart: crossplane
            cmd: lint .
        - name: package
          uses: helm/package@v1
          with:
            chart: ${source.dir}/helm-charts/cluster/charts/crossplane
      paths:
        - type: directory
          path: ${source.dir}/config
          uid: 0
          gid: 0
          mode: "0644"
          source: chart/crossplane/config
