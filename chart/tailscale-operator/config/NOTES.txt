The Tailscale Operator has been installed.

Verify the installation by checking the operator pod status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "tailscale-operator.name" . }}

For more information, see the DHI Tailscale Operator guide:
  https://dhi.io/chart/tailscale-operator/guides
